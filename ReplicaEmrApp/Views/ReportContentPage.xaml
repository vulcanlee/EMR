<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="病歷單明細"
             x:Class="ReplicaEmrApp.Views.ReportContentPage"
             xmlns:viewModel="clr-namespace:ReplicaEmrApp.ViewModels"
             x:DataType="viewModel:ReportContentPageViewModel">

  <Grid>
        <!--<WebView Source="{Binding Url}"
                 VerticalOptions="Fill"
                 HorizontalOptions="Fill"/>-->
        <WebView>
            <WebView.Source>
                <HtmlWebViewSource Html="{Binding HtmlContent}" />
            </WebView.Source>
        </WebView>
    </Grid>

</ContentPage>
